#include "main.h"
/**
 *_isdigit - check the upper case letter
 *@c: function parameters
 * Return:  1 on success else 0.
 */
int _isdigit(int c)
{

if (c >= '0' && c <= '9')
return (1);
else
return (0);

}
